# Taille de la file des requêtes du serveur
slots: 256

# Taille maximale d'une réponse (-1 si pas de limite)
response_limit: -1

# Indique si le serveur est un démon (0 si non, une autre valeur si oui)
daemon: 0